# Flask och MySQL Docker Applikation

## Hur man kör applikationen

1. Klona repositoryt och navigera till projektets rotmapp.
2. Bygg och starta containrarna med Docker Compose:
   ```bash
   docker-compose up --build
